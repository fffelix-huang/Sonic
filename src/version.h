#pragma once

// Includes the string library for working with std::string
#include <string>

// Defines the major version number for the project
#define SONIC_VERSION_MAJOR 1

// Defines the minor version number for the project
#define SONIC_VERSION_MINOR 0

// Function declaration that returns the version as a formatted string
std::string version_to_string();
